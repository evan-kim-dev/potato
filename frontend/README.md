# frontend

GitHub Pages에 배포되는 정적 웹앱입니다.

| 파일 | 역할 |
|------|------|
| `index.html` | 진입점 |
| `app.js` | AI 채팅·플래너·지도·커뮤니티 |
| `data.js` | `backend/data`에서 생성 (직접 수정 금지) |
| `style.css` / `icons.js` / `assets/` | UI |

캐시 버스팅: `index.html`의 `?v=N` 숫자 증가.
